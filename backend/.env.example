# Copia este fichero como .env y rellena los valores
# En Railway estas variables se configuran en el panel de Variables

SECRET_KEY=cambia_esto_por_una_clave_secreta_larga
ALLOWED_ORIGINS=https://tu-app.netlify.app

# El serviceAccountKey.json de Firebase se sube como variable de entorno en Railway:
# GOOGLE_APPLICATION_CREDENTIALS=/app/serviceAccountKey.json
# O bien pega el JSON completo en la variable FIREBASE_SERVICE_ACCOUNT
